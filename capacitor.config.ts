import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.subin.booking-places',
  appName: 'subin-booking-places',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
